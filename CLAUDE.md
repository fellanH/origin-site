# origin-site

Origin product marketing and documentation site.

At session start: `get_context(tags: ["origin-project", "origin-site"])`

## Conventions

- React 19 + Vite 7 + Tailwind v4 + MDX
- TypeScript strict mode
- API docs auto-generated via `npm run docs:generate`
